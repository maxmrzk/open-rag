
  # Build RAG System Builder UI

  This is a code bundle for Build RAG System Builder UI. The original project is available at https://www.figma.com/design/N7qpS9mkQzIvn9FBHj9gEH/Build-RAG-System-Builder-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  